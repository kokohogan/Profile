import 'package:flutter/material.dart';

class PhoneVer extends StatelessWidget {
  const PhoneVer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
